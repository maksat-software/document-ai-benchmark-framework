{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "InvoiceExtraction",
  "description": "Schema for extracted invoice fields used in benchmark evaluation.",
  "type": "object",
  "properties": {
    "invoice_number": {
      "type": ["string", "null"],
      "description": "The invoice identifier or number."
    },
    "invoice_date": {
      "type": ["string", "null"],
      "description": "The invoice date, normalized to YYYY-MM-DD format."
    },
    "vendor_name": {
      "type": ["string", "null"],
      "description": "The name of the vendor or supplier."
    },
    "total_amount": {
      "type": ["number", "null"],
      "description": "The total invoice amount as a numeric value."
    },
    "currency": {
      "type": ["string", "null"],
      "description": "The three-letter ISO 4217 currency code (e.g. USD, EUR)."
    }
  },
  "required": ["invoice_number", "invoice_date", "vendor_name", "total_amount", "currency"],
  "additionalProperties": false
}
